/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/cocukyayini',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

export default nextConfig;
